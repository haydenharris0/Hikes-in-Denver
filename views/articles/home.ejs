<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
         <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
        <meta http-equiv="X-UA-Compatible" content="ie=edge">
        <title>Hikes in Denver</title> 
    </head>
    <header>
        <%- include('_header') %>
    </header>
    <body>
        <div class="container">
            <h1 class="mb-4">List of Hikes</h1>
            <a href="/articles/new" class="btn btn-outline-success">New Hike</a>

            <% articles.forEach(article => { %>
                <div class="card mt-4">
                    <div class="card-body">
                        <h4 class="card-title"><%= article.title %></h4>
                        <div class="card-subtitle text-muted mb-2">Hike was posted on <%= article.createdAt.toLocaleDateString() %></div>
                    </div>
                    <div class="card-text mb-2">
                        Hike length is <%= article.length %> miles and it's rated as <%= article.difficulty %>
                        <div>
                            <a href="articles/<%= article.id %>" class="btn btn-outline-dark">Read More</a>
                            <a href="articles/edit/<%= article.id %>" class="btn btn-outline-secondary">Edit</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    </body>
</html>